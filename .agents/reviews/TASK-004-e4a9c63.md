# TASK-004 AH-029 worktree isolation review

- Agent: `code-review`
- Commit range: `e4a9c63^..e4a9c63`
- Verdict: `CHANGES_REQUESTED`
- Evidence: Complete committed diff and surrounding harness documentation inspected; worktree regression, Bash syntax validation, and diff check passed. Identifier, duplicate, path-root, dirty-worktree, branch-preservation, and generated-state isolation paths are covered, but cleanup does not verify branch ownership.
- Findings: `finish` can remove a clean detached or wrong-branch worktree; `list` can inherit stale branch state for a detached porcelain block.

## Acceptance criteria not proven

- Cleanup must prove the managed path still owns `task/TASK-XXX` and reject
  detached or mismatched branches.
- Detached list output needs explicit, non-stale representation.
